# 计算$\sigma$


# 计算$\hat{x_i}$
已知$\sigma,w$，计算$\hat{x_i}$。
取4x8行32列的向量(bf16) ,8维fp32的$\sigma$, 2x64维bf16的w;

在一个向量寄存器中可以存储128个fp32，所以将$\sigma$拆分成进两组向量寄存器v5 v6, 每一组向量寄存器存储4（个）x32（重复）个fp32；

将w拆分成4组，每组存储32（个）x8（重复）个bf16；

每行对应一个$\sigma$，每列对应一个w。